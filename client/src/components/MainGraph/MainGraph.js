import React from 'react';
import { Box } from '@mui/material';

function MainGraph() {
  return ( 
      <Box bgcolor='pink' flex={4} p={2} border={1}>Main Graph</Box>
   );
}

export default MainGraph;